FROM manjarolinux/base:20230226
RUN pacman -S --noconfirm binutils && \
    cd DataCollection &&
    python -m venv venv && \
    source venv/bin/activate && \
    pip install -r requirements.txt
ARG FILENAME
CMD ["python", "data_collection.py", $FILENAME]